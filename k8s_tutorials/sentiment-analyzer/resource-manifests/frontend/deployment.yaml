apiVersion: apps/v1
kind: Deployment
metadata:
    name: sa-frontend
    labels:
        app: sa
        tier: frontend
spec:
    selector:
        matchLabels:
            app: sa
            tier: frontend
    replicas: 2
    minReadySeconds: 15
    strategy:
        type: RollingUpdate
        rollingUpdate:
            maxUnavailable: 1
            maxSurge: 1
    template:
        metadata:
            labels:
                app: sa
                tier: frontend
        spec:
            containers:
                - name: sa-frontend
                  image: vascoalramos/sa-frontend
                  imagePullPolicy: Always
                  ports:
                      - containerPort: 80
